name: build-test-push

on:
  push:
    branches: ["main"]
  workflow_dispatch: {}

jobs:
  build_test_push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build (linux/amd64) and LOAD into the runner so we can run tests
      - name: Build image (load for smoke test)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          load: true
          tags: localtest:latest
          # Optional: speeds up rebuilds on GitHub runners
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test
        run: |
          set -euxo pipefail

          # Basic sanity: python runs
          docker run --rm localtest:latest python -c "import sys; print(sys.version)"

          # Optional sanity: bbdown exists (doesn't fail the job if it exits nonzero)
          docker run --rm localtest:latest BBDown --help || true

          # Optional sanity: key imports (edit/remove depending on your project)
          docker run --rm localtest:latest python -c "import videocr; print('videocr import ok')"
          docker run --rm localtest:latest python -c "from paddleocr import PaddleOCR; print('paddleocr import ok')"

      # If smoke test passed, rebuild & PUSH to GHCR with proper tags
      - name: Build and push to GHCR
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max